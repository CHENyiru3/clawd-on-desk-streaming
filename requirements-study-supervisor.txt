# Python deps for scripts/supervisor.py (webcam → Clawd POST /supervisor)
# Default backend is OpenCV only; install mediapipe for --backend mediapipe
opencv-python>=4.8.0
requests>=2.28.0
pynput>=1.7.6
mediapipe>=0.10.0
numpy>=1.24.0
